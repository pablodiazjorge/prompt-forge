# ── prompt-forge (drop-in toolkit for AI coding enhancement) ──
# This file only covers prompt-forge's own artifacts.
# Your project should have its own .gitignore for build output, deps, IDE, etc.

# Session logs generated by scripts/session-*.ps1
.prompt-forge/
